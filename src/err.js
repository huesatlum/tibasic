var colors = require('colors');

module.exports = function throwErr (type) {
  // Repetitive now, but leaves room for different error messages
  var types = {
    SYNTAX: 'SYNTAX'
  };

  var message = types[type];
}
